# Core

Owns OCR use cases and deterministic exporters. It depends only on Contracts.

Core does not start processes, access a GUI, register shortcuts or reference a concrete runtime. Those behaviors are supplied through its ports.

