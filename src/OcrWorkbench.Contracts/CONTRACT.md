# Contracts

Owns versioned domain DTOs and the worker wire protocol. It depends on no other solution project.

Consumers may depend on this module. This module may not depend on Core, PluginHost, UI, CLI or a worker implementation.

