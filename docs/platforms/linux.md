# Linux continuation

Linux is scheduled after Windows. The intended adapter uses desktop portals for screenshot and global-shortcut capabilities where available, with explicit unsupported outcomes on desktops that provide neither. Core and worker contracts must remain unchanged.
